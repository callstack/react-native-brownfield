---
'@callstack/react-native-brownfield': major
---

Add Android startup, TTID/TTFD, and JS/UI responsiveness metrics. Android's
`OnJSBundleLoaded` callback now receives `JSBundleTimings` instead of a Boolean.
