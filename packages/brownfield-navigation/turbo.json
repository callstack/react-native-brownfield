{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*"],
      "outputs": ["lib/**"]
    },
    "build:brownfield": {
      "inputs": ["src/**/*"],
      "outputs": ["lib/**"]
    }
  }
}
